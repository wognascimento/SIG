<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:gv="clr-namespace:BarcodeScanner.Mobile;assembly=BarcodeScanner.Mobile.Maui"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             x:Class="ScannerQRcode.Views.ReaderEnderecamento"
             Title="Endereçamwnto Volume Shopping">


    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!--<RowDefinition Height="50" />-->
        </Grid.RowDefinitions>

        <gv:CameraView
            x:Name="Camera"
            OnDetected="Camera_OnDetected" 
            TorchOn="False" 
            VibrationOnDetected="True" 
            ScanInterval="1000"/>

        <Button
            x:Name="send"
            Grid.Row="0"
            Text="Enviar 0 volumes" 
            HeightRequest="40" 
            VerticalOptions="End"
            BackgroundColor="Green"
            CornerRadius="0"
            Clicked="Button_Clicked"/>
    </Grid>


</ContentPage>